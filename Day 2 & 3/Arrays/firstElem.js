// - [ ] Return the first element of an array.

// Using only array

const arr = [16, 14, 12, 19, 32];
console.log(arr[0]);

// using function (arrow func) in array

let firstElem = (arr1) => {
    return arr1[0]
}

console.log(firstElem([1, 2, 3]))